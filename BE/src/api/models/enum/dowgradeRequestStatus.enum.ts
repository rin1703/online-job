export enum DowngradeRequestStatus {
  PENDING_PREVIEW = "pending",
  AWAITING_CONFIRMATION = "awaiting_confirmation",
  APPLYING = "applying",
  APPLIED = "applied",
  CANCELLED = "cancelled",
  EXPIRED = "expired",
}
